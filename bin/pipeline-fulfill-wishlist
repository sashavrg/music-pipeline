#!/usr/bin/env python3
"""Wrapper for pipeline.wishlist_fulfillment.main — see that module."""
import os, sys
sys.path.insert(0, os.environ.get("MUSIC_PIPELINE_ROOT", "/opt/music-pipeline"))
from pipeline.wishlist_fulfillment import main
sys.exit(main() or 0)
