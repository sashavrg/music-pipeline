#!/usr/bin/env python3
"""Wrapper for pipeline.weekly_digest. The package lives under MUSIC_PIPELINE_ROOT
(default /opt/music-pipeline)."""
import os, sys
sys.path.insert(0, os.environ.get("MUSIC_PIPELINE_ROOT", "/opt/music-pipeline"))
from pipeline.weekly_digest import main
sys.exit(main() or 0)
