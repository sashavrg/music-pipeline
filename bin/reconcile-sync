#!/usr/bin/env python3
"""Wrapper for pipeline.reconcile_sync — consolidate beets library.db to the
on-disk shape after a manual Plex cleanup. DRY-RUN by default; --execute writes.
The package lives under MUSIC_PIPELINE_ROOT (default /opt/music-pipeline)."""
import os, sys
sys.path.insert(0, os.environ.get("MUSIC_PIPELINE_ROOT", "/opt/music-pipeline"))
from pipeline.reconcile_sync import main
sys.exit(main() or 0)
